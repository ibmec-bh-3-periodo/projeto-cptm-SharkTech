//const express = require('express');
import express, {Request, Response}  from 'express';

const server = express();

const WHATSAPP_NUMBER = "0800 055 0121";

server.get("/fale-conosco", (req: Request, res: Response) => {

server.listen(5001)